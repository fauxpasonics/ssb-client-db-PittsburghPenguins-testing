SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [etl].[TestFakeLoad]
(
	@BatchId NVARCHAR(50) = '00000000-0000-0000-0000-000000000000',
	@Options NVARCHAR(MAX) = NULL,
	@WaitSecs INT = 0,
	@ForceError BIT = 0
)

AS
BEGIN


	DECLARE @Delay NVARCHAR(10) = CONCAT('00:00:', @WaitSecs)

	WAITFOR DELAY @Delay

	IF (@ForceError = 1)
	BEGIN
		INSERT INTO etl.TestLog VALUES( CONVERT(NVARCHAR(25), GETDATE(), 113) )
	END
	
END


GO
